name: Block enemies on unstable cracks

on:
  push:
    branches:
      - dev

permissions:
  contents: write

jobs:
  migrate:
    if: github.event.head_commit.message == 'Trigger enemy crack collision migration'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout dev
        uses: actions/checkout@v4
        with:
          ref: dev

      - name: Apply collision migration
        run: python3 .github/scripts/block_enemy_cracks.py

      - name: JavaScript syntax
        run: |
          node --check game.js
          node --check dev-tools.js

      - name: Structural guards
        run: node tests/structure.test.cjs

      - name: Gameplay regression suite
        run: node tests/regression.test.cjs

      - name: Diff sanity
        run: git diff --check

      - name: Commit collision fix
        run: |
          git config user.name "Mauro Novellas"
          git config user.email "mauro@novellas.es"
          git add game.js tests/regression.test.cjs
          git commit -m "Block enemies from unstable cracks"
          git push origin HEAD:dev

# trigger
